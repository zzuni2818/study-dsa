# download curses library
	sudo apt-get install libncurses5-dev libncursesw5-dev

# download curses man-page
	sudo apt-get install ncurses-doc

# how to build
	gcc -o right_hand_on_wall.out right_hand_on_wall.c -lncursesw
